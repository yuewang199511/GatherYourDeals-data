# Summary

This article records the load testing results from run #5, the first successful load test run against Azure Container Apps + PostgreSQL Flexible Server via pgBouncer (session mode).

Service was deployed on Azure Container Apps (Consumption plan, `westus3`) backed by Azure PostgreSQL Flexible Server (`gyd-pg-main`) via pgBouncer Container App (`gyd-pgbouncer`).

**6 of 8 phases valid; 1 phase had a test infrastructure defect; 1 phase failed.** The `read_heavy/moderate` phase suffered a login stampede that collapsed users from 400 → 64, making its results invalid (tested at 16% intended capacity). The `read_heavy/stress` phase failed with 82 read timeouts (4.96% error rate) due to pgBouncer connection pool saturation — this result is still valid. All other 6 phases passed cleanly, including `write_ops/stress` (500 users, 716 RPS).

> **Post-run investigation (2026-03-30):** `read_heavy/moderate` numbers are suspicious and confirmed invalid. See [Post-Run Investigation](#post-run-investigation) section below.

# Environment

- **Platform**: Azure Container Apps (Consumption plan, `westus3`)
- **Database**: PostgreSQL Flexible Server `gyd-pg-main` via pgBouncer (session mode)
- **Service**: `GatherYourDeals-data` (branch `ci/azure-load-test-persistent`)
- **GitHub Run**: `23725032367` (artifact: `load-test-results-23725032367`)
- **Results directory**: `load_testing/results/20260330_022049/`
- **Note**: `HONEYCOMB_API_KEY` present in CI — Locust context events posted to dataset `gatheryourdeals-load-tests`

## Changes from Run 4 (Railway)

- **Platform**: Switched from Railway to Azure Container Apps (Consumption plan)
- **DB connectivity**: Azure PostgreSQL via pgBouncer Container App (same session mode as Railway run 3/4)
- **CPU**: Azure Consumption plan CPU allocation (shared/burstable); Railway run 4 used explicit 8 vCPUs
- **CI fixes applied this run**: Added `AllowAllAzureIps` PostgreSQL firewall rule + pgBouncer restart after secret update

# Performance Result

**Test run IDs:**
- cpu_bound/moderate: `501d55c8-b650-4dea-8e22-a7ebf76d9562` ✓
- cpu_bound/stress: `abc13c80-9f20-41af-be09-10d42ae48246` ✓
- read_heavy/moderate: `cfb23341-1cbc-4204-abf1-72a52954b270` ⚠ (invalid — login stampede, ran at 16% capacity)
- read_heavy/stress: `a276f989-46a9-4057-ad0c-c769cdf7bbee` ✗ (4.96% error rate — read timeouts)
- write_ops/moderate: `f157274f-a3bd-4fa5-9742-7338d997babd` ✓
- write_ops/stress: `f729a69e-bafb-4608-b030-5bd37612e42b` ✓
- misc_lightweight/moderate: `8fbf551e-e978-4f50-b2a8-f3504800ce29` ✓
- misc_lightweight/stress: `7372cebd-1bba-49e5-a614-75fe30dfcaab` ✓

**Time Range**: 2026-03-30 02:20 UTC → 02:40 UTC (~20 min total)

## Run Duration

Users = Locust worker count (derived from target RPS; see note below table).

| Group | Phase | Users | Target RPS | Intended (s) | Actual (s) | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| cpu_bound | moderate | 100 | 100 | 120 | ~131 | ✓ 0 errors |
| cpu_bound | stress | 500 | 500 | 150 | ~149 | ✓ 0 errors |
| read_heavy | moderate | 400 → **64** | 100 | 120 | ~122 | ⚠ invalid — login stampede (see below) |
| read_heavy | stress | 2000 | 500 | 150 | 151.3 | ✗ 82 read timeouts |
| write_ops | moderate | 100 | 200 | 120 | ~122 | ✓ 0 errors |
| write_ops | stress | 500 | 1000 | 150 | ~150 | ✓ 0 errors |
| misc_lightweight | moderate | 110 | 100 | 120 | ~122 | ✓ 0 errors |
| misc_lightweight | stress | 540 | 500 | 150 | ~150 | ✓ 0 errors |

*User–RPS relationship: each user targets 1 task/sec (`constant_throughput(1.0)`). For groups with N equal-weight endpoints, `users = target_rps_per_endpoint × N`. Target RPS shown for write_ops is total HTTP throughput (2 req per cycle × cycles/s); for all other groups it is per-endpoint.*

## Moderate Phase

| Endpoint | Target RPS | Actual RPS | Avg Latency (ms) | P50 / P95 (ms) | # Errors | # Requests |
| --- | --- | --- | --- | --- | --- | --- |
| POST /api/v1/auth/login | 100 | 7.4 | 12,808 | 12,000 / 20,000 | 0 | 876 |
| GET /api/v1/auth/me | 100 | 12.0 ⚠ | 28 | 17 / 18 | 0 | 1,464 |
| GET /api/v1/meta | 100 | 11.2 ⚠ | 214 | 18 / 21 | 0 | 1,356 |
| GET /api/v1/receipts | 100 | 12.2 ⚠ | 159 | 19 / 24 | 0 | 1,478 |
| GET /api/v1/receipts/:id | 100 | 11.3 ⚠ | 166 | 18 / 22 | 0 | 1,377 |
| POST /api/v1/receipts | 100 | 90.5 | 54 | 27 / 150 | 0 | 10,892 |
| DELETE /api/v1/receipts/:id | 100 | 90.5 | 41 | 28 / 91 | 0 | 10,892 |
| POST /api/v1/auth/refresh | 100 | 77.4 | 77 | 45 / 190 | 0 | 9,312 |
| POST /api/v1/meta | 5 | 3.7 | 55 | 25 / 160 | 0 | 450 |
| PUT /api/v1/meta/:fieldName | 5 | 4.0 | 41 | 25 / 170 | 0 | 480 |

*⚠ read_heavy/moderate ran with only 64 of 400 intended users due to login stampede — actual RPS reflects 16% capacity, not the 100 RPS target. See [Post-Run Investigation](#post-run-investigation).*

*Note: Login actual RPS (7.4) is lower than Railway run 4 (36 RPS) due to burstable/shared CPU on Azure Consumption plan vs Railway's dedicated 8 vCPUs. bcrypt at cost 12 is CPU-bound.*

## Stress Phase

| Endpoint | Target RPS | Actual RPS | Avg Latency (ms) | P50 / P95 (ms) | # Errors | # Requests |
| --- | --- | --- | --- | --- | --- | --- |
| POST /api/v1/auth/login | 500 | 6.3 | 38,224 | 34,000 / 85,000 | 0 | 941 |
| GET /api/v1/auth/me | 500 | 3.0 | 307 | 17 / 880 | 0 | 454 |
| GET /api/v1/meta | 500 | 2.7 | 8,060 | 19 / 60,000 | **31** | 407 |
| GET /api/v1/receipts | 500 | 2.5 | 8,011 | 20 / 60,000 | **30** | 382 |
| GET /api/v1/receipts/:id | 500 | 2.7 | 6,180 | 18 / 60,000 | **21** | 410 |
| POST /api/v1/receipts | 500 | 358.1 | 167 | 60 / 700 | 0 | 53,886 |
| DELETE /api/v1/receipts/:id | 500 | 358.1 | 148 | 47 / 670 | 0 | 53,886 |
| POST /api/v1/auth/refresh | 500 | 27.8 | 2,205 | 76 / 6,400 | 0 | 4,174 |
| POST /api/v1/meta | 20 | 1.5 | 927 | 31 / 1,000 | 0 | 226 |
| PUT /api/v1/meta/:fieldName | 20 | 1.5 | 1,140 | 33 / 4,200 | 0 | 224 |

## Failures

| Phase | Endpoint | Error | Count |
| --- | --- | --- | --- |
| read_heavy/stress | GET /api/v1/receipts | RetriesExceeded (read operation timed out) | 30 |
| read_heavy/stress | GET /api/v1/meta | RetriesExceeded (read operation timed out) | 31 |
| read_heavy/stress | GET /api/v1/receipts/:id | RetriesExceeded (read operation timed out) | 21 |

All 82 failures are `RetriesExceeded` client-side read timeouts at the 60-second threshold. `GET /api/v1/auth/me` (JWT-only, no DB query) had 0 failures — confirming the timeouts are DB-related, not network.

## Root Cause Analysis — read_heavy/stress Failure

**Low actual RPS (3 RPS vs 500 target):**

At 2000 users under pool saturation, most requests queue for connections. Threads are occupied waiting rather than executing, so effective throughput collapses. The low actual RPS is a symptom of the saturation failure, not an independent issue.

**Bimodal latency distribution:**

The P50 for all read endpoints is 17–20ms — extremely fast, consistent with a healthy pgBouncer pool. The P95 jumps to 60s (the Locust timeout). This bimodal pattern (fast P50, timeout P95) is the classic signature of **connection pool queue overflow**: requests that acquire a connection immediately complete in ~20ms; requests that queue for more than 60 seconds time out.

**PostgreSQL connection limit:**

Azure PostgreSQL Flexible Server's default `max_connections` varies by SKU. On the Burstable `B1ms` tier (the cheapest tier often provisioned in CI), `max_connections = 50`. pgBouncer in session mode holds one backend connection per active client. With 2000 Locust users all issuing reads, pgBouncer's client pool size can reach hundreds while only 50 backend connections are available — causing the queue overflow.

**Fix path:**

Switch pgBouncer from session mode to **transaction mode** (requires `QueryExecModeSimpleProtocol` on the pgx driver, same as the TODO from run 4). In transaction mode, a backend connection is held only for the duration of a transaction, not the entire client session — allowing the 50 backend connections to serve thousands of concurrent clients without queuing. Alternatively, upsize the PostgreSQL SKU to increase `max_connections`.

## Comparison vs. Previous Runs

| Metric | Run 1 (Local, SQLite) | Run 3 (Railway, PG) | Run 4 (Railway, PG) | Run 5 (Azure, PG) |
| --- | --- | --- | --- | --- |
| Login P50 (moderate) | 790ms | 2,600ms | 2,700ms | 12,000ms |
| Login actual RPS (moderate) | 93 | 36.1 | 36.45 | 7.4 |
| Read P50 (moderate) | 1–4ms | 130ms | 70–100ms | 17–19ms |
| Read P50 (stress) | 4–110ms | 130–160ms | 74–160ms | 17–20ms (P50) / 60,000ms (P95) |
| Write P50 (moderate) | 1–2ms | 130ms | 79–81ms | 27–28ms |
| Write P50 (stress) | 2ms | 130ms | 120–140ms | 47–60ms |
| Login moderate error rate | 0% | 0% | 0% | 0% |
| Read stress error rate | 0% | 0% | 0% | **4.96% (pool saturation)** |
| Total errors | 0 | 0 | 509 (burst) | 82 (timeouts) |
| Stress phase pass rate | 4/4 | 4/4 | 3/4 | 3/4 |

**Notable Azure findings:**
- Read latency at moderate load (P50 17–19ms) is **faster than Railway** (70–130ms) — likely due to pgBouncer being co-located in the same Azure region/env as the service
- Write throughput at stress (716 RPS combined) exceeds Railway run 4 — strong result
- Login throughput is lower due to Consumption plan CPU limits (not a concern for production deployments with dedicated CPU)

## TODO

- [ ] Switch pgBouncer from session mode to transaction mode + set `QueryExecModeSimpleProtocol` on pgx driver, to fix the `read_heavy/stress` connection pool saturation. Expected to resolve the 4.96% timeout rate.
- [ ] Alternatively: upsize `gyd-pg-main` PostgreSQL SKU to increase `max_connections`.
- [ ] Fix `make_shape()` spawn rate for moderate phase — cap to a sane rate (e.g. 10–20 users/sec) so `on_start` logins are staggered, preventing the login stampede that invalidated `read_heavy/moderate`.
- [ ] Re-run `read_heavy/moderate` after the spawn rate fix to get valid baseline data at 400 users.

---

## Post-Run Investigation

**Investigation date:** 2026-03-30

### Findings

After reviewing the artifact from GitHub Run `23725032367`, two anomalies were identified in `read_heavy/moderate`:

#### 1. User Count Collapse: 400 → 64 (Test Infrastructure Bug)

The `stats_history.csv` shows the user count dropped from 400 → 314 → 64 within the first 30 seconds of the test:

```
t=0s:  400 users spawned (spawn_rate=400/sec — all at once)
t=12s: first request completes (830ms response time)
t=30s: 314 users remaining
t=31s: 64 users — stable for remainder of 120s test
```

**Root cause:** `make_shape()` returns `(_moderate_users, _moderate_users)` for the moderate phase, which for group2_reads means `(400, 400)` — spawn 400 users at 400/second (effectively instantaneous). All 400 `on_start()` calls immediately invoke `safe_login()`, which calls `login()` with a 30-second timeout. The login endpoint is CPU-bound (bcrypt cost 12). On Azure Consumption plan (burstable CPU), the login endpoint handles only ~7.4 RPS at 100 concurrent users. Under 400 simultaneous login requests, throughput collapses further — most requests queue past the 30-second timeout, triggering `StopUser`. Only 64 users completed `on_start` in time.

Other groups (cpu_bound=100 users, write_ops=100, misc_lightweight=110) are unaffected — their smaller spawn counts stay within the login capacity.

**Impact:** `read_heavy/moderate` ran at 16% of intended capacity (64 of 400 users). Actual RPS was ~12–16 per endpoint; intended was ~100. The results are not representative of 400-user moderate load.

#### 2. P95 Lower Than Average (Statistical Contamination)

For `GET /api/v1/meta` moderate: **Avg=214ms, P50=18ms, P95=21ms**.

This is statistically valid but misleading. The cumulative stats include both:
- **Steady state (64 users, ~90s):** P50=18ms, P95=21ms — clean
- **Stampede window (400 users, ~30s):** a few requests with 9,700–23,180ms response times (P99=9700ms, P99.9=21,000ms, max=23,180ms)

With 1,356 total requests, approximately 14 requests averaging ~12,000ms inflate the cumulative mean from ~18ms to ~214ms. The P95 correctly reflects the 64-user steady state; the mean is contaminated by the stampede window. This pattern repeats across all four `read_heavy/moderate` endpoints.

#### Comparison with Other Groups

| Group | Phase | Intended Users | Actual Users | Stable? |
| --- | --- | --- | --- | --- |
| cpu_bound | moderate | 100 | 100 | ✓ |
| read_heavy | moderate | 400 | 64 | ✗ stampede |
| write_ops | moderate | 100 | 100 | ✓ |
| misc_lightweight | moderate | 110 | 110 | ✓ |

Only `read_heavy/moderate` is affected because it is the only moderate group with 400 users.

### Fix Plan

**In `load_testing/locust/common.py` → `make_shape()`:**

The moderate branch returns `(_moderate_users, _moderate_users)` as spawn rate. This must be capped to prevent stampede:

```python
# Before (broken):
return _moderate_users, _moderate_users

# After:
_moderate_spawn_rate = min(_moderate_users, 20)  # cap at 20/sec
return _moderate_users, _moderate_spawn_rate
```

A spawn rate of 20 users/sec for 400 users takes 20 seconds of ramp, staggering logins and keeping load within the ~7.4 login RPS capacity. The stress phase already uses a proper ramp and is unaffected.


